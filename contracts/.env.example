# Foundry deploy env (DO NOT COMMIT real keys)

# Mantle Sepolia (5003)
MANTLE_SEPOLIA_RPC_URL=https://rpc.sepolia.mantle.xyz

# Deployer private key (0x...)
PRIVATE_KEY=

# Optional: use a different Entropy contract address
# ENTROPY_ADDRESS=0x...

# Optional: restrict upkeep execution to a keeper/forwarder
# AUTOMATION_FORWARDER=0x...

# Optional: deploy on-chain MockEntropy instead of real Pyth Entropy
# DEPLOY_MOCK_ENTROPY=true
